#include <stdio.h>

int main() { 

	//
	int size;
	int hash[size];
	int number_cases;
	int keys;
	int address;

	scanf("%d", &number_cases);
	scanf("%d", &keys);
	scanf("%d", &size);
	for (int i = 0; i < size; i++) { 
		scanf("%d", hash[i]);
	}	






}
